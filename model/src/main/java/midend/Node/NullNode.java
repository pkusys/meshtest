package midend.Node;


import midend.Exp.Exp;

// NullNode is a node that does nothing
// For example, start and end nodes
public class NullNode extends Node{
    public NullNode(String msg){
        super(msg);
    }
}
